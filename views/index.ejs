<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Chats</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <h2 style="color : #595959">All Chats!</h2>
    <form method="GET" action="/chats/new">
        <button style="background-color : #595959 ; color:rgb(229, 223, 223)">
            New Chat
        </button>
    </form>
    <br>
   <% for (let chat of chats){ %>
        <div class="chat">
            <p>From :<b> <%= chat.from %></b></p>
            <div class="msg">
                <p> Message : <%= chat.msg %></p>
                <form method="GET" action="/chats/<%= chat._id %>/edit">
                    <button  style="background-color : rgb(229, 223, 223) ; color:#595959">Edit</button>
                </form>
                <form method="POST" action="/chats/<%= chat._id %>?_method=DELETE">
                    <button  style="background-color : rgb(229, 223, 223) ; color:#595959">Delete</button>
                </form>

            </div>
            <p>Received By : <b><%= chat.to %></b> </p>
            <hr>
            <p><%= chat.created_at.toString().split(" ").slice(0,4).join(" ")%></p>
            <p><%= chat.created_at.toString().split(" ")[4] %></p>
        </div>

        </div>
 <%   }  %>

    </body>
</body>
</html>